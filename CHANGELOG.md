# 版本紀錄

版本紀錄以 `js/version.js` 的 `ENV_VERSION_HISTORY` 為唯一來源；此檔為摘要。

## v1.13.0（2026-09-23）

- ⑨ 新增「測站」選單：可選環境部任一空品自動測站（「更新測站清單」取得約 80 站），各測站資料分開存放，圖例名稱各自設定。
- 自動抓取改為抓該測站的全部測項（不再只有 PM10、PM2.5）；匯入 CSV 也保存全部測項，依檔案裡的測站自動分開。
- 新增「下載這個測站的原始數據」：Excel（逐時表＋與環境部相同欄位的原始資料）或 CSV（與環境部格式相同），數值原樣保留，不用畫趨勢圖。
- 趨勢圖測項改為勾選（預設 PM2.5、PM10），每個測項一張圖；可畫 SO₂、NO₂、NO、NOx、CO、O₃、溫度、濕度、風速、雨量等（測站有該測項才有紅線）；Y 軸範圍每個測項可各自設定。
- 月報匯入可辨識氣體類欄位（SO₂、NO₂、NO、NOx、CO、CO₂、O₃、THC、NMHC、CH₄）。
- 圖上感測器名稱重複時自動加上編號（盒鬚圖、趨勢圖）。
- v1.12 以前匯入的環境部資料（彰化站 PM10、PM2.5）自動轉換成新格式。
- 報表計算方式不變。

## v1.12.3（2026-09-23）

- 網站名稱由「環境監測季報產生器」改為「微型感測器數據系統」（頁面標題、瀏覽器分頁名稱、使用說明、README）；副標題補上盒鬚圖與環境部比對趨勢圖。
- 資料、設定、備份檔都不受影響（內部識別碼不變，舊備份照樣可還原）。

## v1.12.2（2026-09-23）

- 新增給維護者（含 AI）的說明：README.md「給維護者（含 AI）：常見維護事項」，以及 AI 工具會自動讀取的 AGENTS.md、CLAUDE.md；js/trend.js 網址設定處加上醒目註解。
- 自動抓取失敗的訊息會寫出網址設定的位置（js/trend.js 的 DEFAULT_API），截圖給 AI 時就會帶到。
- 計算方式不變。

## v1.12.1（2026-09-23）

- 「從環境部網站自動抓取」拿掉畫面上的網址與金鑰設定，改為文字說明：抓不到時先按「測試連線」；若環境部網址變更，截圖錯誤訊息並附上新網址，找 AI 協助修改（網址與金鑰寫在 js/trend.js）。
- 計算方式不變。

## v1.12.0（2026-09-23）

- 「⑨ 環境部比對趨勢圖」新增「從環境部網站自動抓取」：選月份後直接向環境部環境資料開放平臺（AQX_P_221 彰化站）抓 PM10、PM2.5，每頁 1000 筆自動翻頁，存法與匯入 CSV 相同；有「測試連線」。
- 「網址與金鑰設定」：API 金鑰與網址範本（{key}、{from}、{to}、{item}、{offset}）可修改並儲存（每個計畫各自保存），有「填回預設網址」。
- 抓不到時顯示原因，可改用 CSV 匯入。
- 報表計算方式不變。

## v1.11.0（2026-09-23）

- 新增「⑨ 環境部比對趨勢圖」分頁：
  - 匯入環境部「空氣品質小時值」CSV（可多檔），只讀 PM10、PM2.5，x 等無效值不計；各月匯入狀況表、刪除某月；圖例名稱可改；每個計畫各自保存、隨備份保存。
  - 期間可選單月、月份區間、季別區間、自訂日期；資料可選日平均或逐時。
  - 每台感測器與環境部測站畫在同一張圖（PM2.5、PM10 各一張），環境部為紅色粗線、畫在最上層；圖例在上方自動換行；日期標籤自動間隔不重疊，下方有月份列。
  - Y 軸自動上限不為少數異常高值拉高（超過的在圖頂端截斷並提示），也可自訂。
  - 下載高解析度 PNG（單張或 zip）與可編輯 Excel（原生折線圖＋資料＋說明）。
- 報表計算方式不變。

## v1.10.0（2026-09-23）

- 新增「⑧ 盒鬚圖」分頁：期間可選單月、月份區間、季別區間、自訂日期；每台感測器的逐時有效數值（和報表相同的採用規則）畫成盒鬚圖，溫度、相對濕度、PM10、PM2.5、TVOC 各一張，噪音 Leq 日間／晚間／夜間各一張。
- X 軸為 ⑤ 設定的感測器名稱（依名稱排序、可勾選要畫的感測器並記住），名稱過長自動斜放；圖上不寫統計數字，Y 軸刻度數字可關閉；標題、X 軸名稱、Y 軸範圍可設定。
- 盒鬚規則同 Excel：四分位數「包含中位數」（QUARTILE.INC），鬚到 1.5 倍四分位距內最遠的數值，預設不顯示離群值與平均值。
- 下載：高解析度 PNG（單張或全部 zip）、可編輯 Excel（原生盒鬚圖＋資料＋統計＋說明）。
- 新增 vendor/jszip.min.js（MIT）。
- 報表計算方式不變。

## v1.9.0（2026-09-23）

- 噪音管制標準：在「④ 產出報表」空氣品質標準下一行，可設定日間、晚間、夜間標準值（出廠預設 71、69、63 dB(A)），每個計畫各自保存。
- 下載的噪音報表中，Leq 日／晚／夜「大於」標準值的格子設為粗體＋底線（等於不標，「－」不標）；下載完成訊息顯示標示格數；「時段說明」工作表寫出使用的標準值。
- 計算方式與數值不變。

## v1.8.5（2026-09-23）

- 最頻風向只寫方位，不加「風」字：「西南風」→「西南」、「北風、西北風」→「北、西北」。「<0.3」「－」不變。
- 計算方式與其他數值不變。

## v1.8.4（2026-09-23）

- 計算方式修正：最頻風向採計「風速 ≥ 0.3 m/s」的小時（剛好 0.3 也計入），只有小於 0.3 不計。原本是「> 0.3」。
- 靜風「<0.3」：全天有測值的小時風速都小於 0.3 才寫。
- 備註文字改為「最頻風向採計 N 小時（風速≥0.3）」「全天風速都 < 0.3 m/s（靜風 N 小時）」。
- 風速平均不受影響（本來就採計所有有效風速）；其他測項、噪音不變。

## v1.8.3（2026-09-23）

- 最頻風向：當日風向、風速都有測值，但每小時風速都 ≤ 0.3 m/s（靜風）時，寫「<0.3」（置中），備註加「全天風速都 ≤ 0.3 m/s（靜風 N 小時），最頻風向記為「<0.3」」。原本是空白／「－」。
- 「－」只代表設備異常或維護沒有測值。
- 其他數值不變。

## v1.8.2（2026-09-23）

- Excel 報表（空品、噪音）：感測器有這個測項、但當日沒有有效數值時，儲存格寫「－」並置中（原本是空白）。感測器沒有這個測項（例如沒有雨量計、沒有風速風向）仍留白，方便分辨。
- 數值與備註不變。

## v1.8.1（2026-09-23）

- 「③ 刪除某個月的資料」：改成「年」「月」兩個下拉選單，月只列出該年已匯入的月份；預設選最新的月份。按刪除後的確認文字會寫出感測器台數與筆數。
- 新增／修改計畫：點計畫編號或名稱欄時自動清空，灰字顯示原本的內容；沒有輸入就離開欄位，會自動恢復原本的內容。
- 計算方式不變。

## v1.8.0（2026-09-23）

- 「② 資料異常確認」：每一區（總覽、月報備註時段、疑似異常、手動新增不採用時段、資料缺漏）的標題點一下可收合／展開。
- 「② 資料異常確認」新增快速跳轉列：捲動時固定在畫面上方，只在 ② 出現；可跳到各區（會自動展開）、回頂端、全部收合、全部展開，並顯示待處理數量。
- 「③ 已匯入資料」：已匯入月份預設只顯示最近 6 個月，按「顯示全部 N 個月」展開；較早月份有缺資料時會提示。
- 可收合的長清單：⑤ 感測器編號與名稱、感測器啟用與停用（超過 25 台預設收合）、對照表匯入的修改清單、手動不採用時段清單（超過 10 段）、匯入時各檔案的檢查結果（超過 3 份，全部沒問題時預設收合）、新的感測器清單（超過 8 台）。
- 計算方式不變。

## v1.7.1（2026-09-23）

- 「② 資料異常確認」的「資料缺漏與無效測值」移到頁面最下方（在「手動新增不採用時段」之後），清單預設收合，只顯示統計；按「展開清單」或點總覽第 ⑤ 項才打開。
- 總覽項目順序改成和頁面相同：① 月報備註時段 ② 疑似異常 ③ 手動不採用 ④ 自動不計的測值 ⑤ 資料缺漏。
- 計算方式不變。

## v1.7.0（2026-09-23）

- 新增的感測器：匯入時自動加入（名稱取自月報表頭），確認畫面列出編號與名稱；之後才裝的，自動以第一筆資料那天為啟用日。
- 少了某一台感測器：確認畫面逐台提示，可選「資料少匯入，之後再補」「感測器已停用（停用日）」「那時還沒啟用（啟用日）」。已停用的感測器又出現資料時會提醒。
- 「⑤ 感測器編號與名稱」新增「感測器啟用與停用」：可修改啟用日、停用日。
- 啟用日以前、停用日當天起：報表不補空白日，「② 資料異常確認」不列為缺漏；有資料的日子照樣列出。
- 修正：修改感測器名稱、表頭文字變動時，會保留啟用日／停用日。
- 目前資料沒有設定啟用／停用日，報表數值與備註與 v1.6.0 完全相同。

## v1.6.0（2026-09-23）

- 「② 備註時段確認」改名為「② 資料異常確認」，最上方新增「資料異常總覽」：月報備註時段、疑似異常、資料缺漏、自動不計的測值（PM 為 0、PM2.5 大於 PM10）、手動不採用，一次看到數量，點一下跳到該區。
- 新增「資料缺漏與無效測值」清單：整月沒有資料、缺少小時（整列沒有）、測值空白或無效；可依類型、感測器、月份篩選，附月報備註。分頁上灰色「缺 N」＝整月沒有資料＋缺少小時的段數。只供檢查，計算方式不變。
- 空品備註：月報少了某幾個小時時寫「月報缺少 8/5 03～05 點（3 小時）的資料，不列入」。
- 噪音備註：缺少的小時分原因寫「月報沒有這幾個小時」或「尚未匯入」。
- 目前資料沒有缺少的小時，所以報表數值與備註和 v1.5.1 完全相同。

## v1.5.1（2026-09-23）

- 噪音時段設定獨立成「⑦ 噪音時段設定」分頁；「④ 產出報表」顯示目前時段（出廠預設／已自訂）並可一鍵前往修改。
- 夜間跨到翌日時，備註寫清楚：
  - 期間最後一天：翌日凌晨沒有資料時寫「夜間應採計 9 小時，其中 9/1 00～06 點 沒有資料（可能是尚未匯入），只採計有資料的 2 小時」。
  - 期間第一天：寫「本日 8/1 00～06 點 屬於前一日（7/31）的夜間，前一日不在本報表期間內，這 7 小時沒有列入本報表」。
- 期間中間的日子，若某時段只有部分小時沒有資料，也用同樣格式說明。
- 出廠預設下所有數值與備註與 v1.5.0 完全相同。

## v1.5.0（2026-09-23）

- 噪音日／晚／夜時段可自訂（每個計畫各自保存；v1.5.1 起在 ⑦ 噪音時段設定）：每個時段 1～3 段，起點與終點可選「當日／翌日」與整點（例：夜間 8/1 22:00～8/2 07:00）。
- 內建三種預設：出廠預設（與原本相同）、夜間跨日 22–翌日 07、夜間跨日 22–翌日 06。
- 會檢查：結束早於開始、超過 24 小時（擋下）；一天中有整點沒被涵蓋或被重複涵蓋（提醒）。
- 噪音報表多一張「時段說明」工作表。
- 出廠預設下所有數值與 v1.4.3 完全相同。

## v1.4.3（2026-09-23）

- 匯入時若和已匯入的資料重複：明確提示「已有 X 筆資料重複」，逐月列出重複筆數、感測器台數、內容不同的筆數；可選「覆蓋重複的資料並匯入」「只匯入新資料（重複的保留原本）」「取消匯入」。
- 計算方式不變。

## v1.4.2（2026-09-23）

- 疑似異常時段：按「確認不採用／改回採用／改回待確認」後畫面停在原地，不再跳回頂端；結果改在右下角提示。
- 反應加快：只改確認結果時不重新讀取全部資料，「採用與不採用的差別」每段只算一次。
- 每段左上角可「勾選」，可「全選目前列出的段」，最後一次決定確認不採用／改回採用／改回待確認。
- 計算方式不變。

## v1.4.1（2026-09-23）

- 疑似異常時段：批次處理新增「全部改回待確認」；和目前「顯示」狀態相同的批次按鈕會自動隱藏（例如顯示「已確認不採用」時不顯示「全部確認不採用」）。計算方式不變。

## v1.4.0（2026-09-23）

- 新增「計畫」：可新增／切換／修改／刪除，每個計畫的資料、確認結果與設定完全獨立（各用一個瀏覽器資料庫）；舊資料自動成為「未命名計畫」。
- 疑似異常改為匯入後自動判定、**預設不採用**，逐段「確認不採用／改回採用／改回待確認」，可批次處理；只排除符合門檻的小時。門檻全部可調、可關閉，並新增溫度、風速、風向、時雨量。匯入前的確認畫面會先列出段數。報表備註寫「自動判定異常不計」。
- 報表：PM10、PM2.5 日平均大於標準值（預設 75、30，可改）設為粗體＋底線；報表與備份檔名加上計畫編號。
- 備註時段確認：「只顯示」可只列出某測項還在採用中的列。
- ⑥：顯示儲存空間、自動申請持久保存、清除目前計畫資料／清除全部。

## v1.3.0（2026-09-23）

- 新增「疑似異常提醒」（在②頁）：Leq 999 以上／高於 120／低於 30 dB、PM 990 以上、濕度超過 100%、PM10／PM2.5／TVOC／濕度同值連續 12 小時以上。每段附狀況說明、數值範圍、不採用後的報表變化，以及建議做法；可一鍵帶入手動不採用設定，或標記為真實數值不再提醒。
- 只提醒、不自動排除；計算方式不變。

## v1.2.1（2026-09-23）

- 備註時段確認：感測器、月份下拉只列出目前顯示狀態（待確認／已確認／全部）下還有資料的選項，並附筆數；某台感測器全部確認完就從「待確認」下拉消失，已選的會自動回到「全部」。計算方式不變。

## v1.2.0（2026-09-23）

- 新增計算規則：同一小時 PM2.5 大於 PM10 時，PM10 與 PM2.5 都不計（報表頁可關閉）；備註寫「PM2.5大於PM10不計：N小時」。與舊 Access 報表相比多影響 12 格。
- 新增「手動新增不採用時段」：指定感測器、起訖時間、測項，一次不採用；可刪除；都會列出重新計算前後的變動。
- 「⑤ 感測器編號與名稱」：下載範本（含目前設定）、匯入對照表（範本／自訂兩欄表／舊報表皆可），可修改報表顯示的感測器編號與名稱；匯入前先列出變更並擋下撞號。
- 匯入新月份時保留使用者設定的報表編號與名稱。

## v1.1.1（2026-09-23）

- 修正：匯入完成訊息的待確認備註時段筆數用到匯入前的資料（匯入 8 月後顯示 37，實際 118）。分頁上的紅色數字一直是對的。計算方式不變。

## v1.1.0（2026-09-23）

- 備註時段確認加入批次處理：月報備註篩選（全選／關鍵字）、表頭「選取」全選、指定測項一次設為不採用或改回採用。
- 計算方式不變。

## v1.0.0（2026-09-23）

- 第一版：由 Access 資料庫「微型感測器監測資料庫.accdb」移植。
- 匯入數據月報（依工作表內容判讀、重複匯入覆蓋、重複時間擋下）。
- 備註時段確認：月報備註欄有文字且仍有有效數值的時段以紅框提醒，可逐格勾選不採用，也可用備註篩選（含關鍵字）＋全選＋指定測項批次設定不採用，確認後重新計算並列出變動。
- 報表期間：季別、月份區間、自訂日期、全部累積。
- 口徑變更（使用者裁示）：一般四捨五入；負值一律無效；最頻風向只採計風速 > 0.3 m/s、並列最多全部列出、方位文字加「風」（例：北風、西北風）；新增日累積雨量與有效資料時數備註。
